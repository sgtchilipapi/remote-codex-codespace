Status button gets lost in small screens. Put it as an icon button on the right side of "send" instead.

Opening the app without an active session shows an open config already. This is wrong as when there is no session yet, ofcourse there would be no model, reasoning and permissions set. Also, when there is no session yet, chat controls should be disabled. Hitting new should enable it while showing on the status section "You are on a new session. <Truncated Session ID>, model, reasoning, permissions. The collapsible panel should only be shown when configure is clicked. Configure button should just be an icon button instead.

AI responses gets appended over a single chat bubble. Each response returned should have a bubble on its own.